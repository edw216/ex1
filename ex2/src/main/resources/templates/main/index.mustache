<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>Board page</title>
    <script>
        var computer_number =0;
        var user_number =0;
        var count = 6;
        function Com_random(){
            computer_number = Math.floor(Math.random()*(100-1)+1);
        }
        Com_random();
        function Input(){
            user_number = document.getElementById("User_Input").value;
            if(count == 0){
                document.getElementById("result_id").innerText="기회를 모두 사용했습니다. 탈락입니다.";
                document.getElementById("result_id2").innerText="";
            }
            else if(parseInt(computer_number)!==parseInt(user_number)) {
                if(parseInt(computer_number)>parseInt(user_number)){
                    document.getElementById("result_id2").innerText="정답은 더 큰 숫자 입니다.";
                }else if(parseInt(computer_number)<parseInt(user_number)){
                    document.getElementById("result_id2").innerText="정답은 더 작은 숫자 입니다.";
                }
                count = count-1;
                document.getElementById("result_id").innerText=count + "번의 기회가 남았습니다.";
            }else{
                document.getElementById("result_id").innerText="정답입니다.!";
                document.getElementById("result_id2").innerText="정답:"+computer_number;
            }
        }

    </script>
</head>
<body>

    <h1>숫자 맞추기 !</h1>

    <h2>컴퓨터가 생성한 숫자를 맞추시오.</h2>
    <input name="User_result1" type="number" id="User_Input"min="1"max="100"/>
    <input type="button" onclick="Input()"value="입력" />
    <br/>
    <h2 id="result_id"></h2>
    <h2 id="result_id2"></h2>
    <br /><br />

    <br /><br />
<hr />
</body>
</html>